{
  "name": "aws-dynamodb-doc-client-wrapper",
  "version": "1.0.1",
  "description": "Wrapps AWS DynamoDB document client for either DynamoDB client or DAX client",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "dax",
    "dynamodb",
    "cache",
    "amazon"
  ],
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/ynevet/dynamodb-doc-client-wrapper"
  },
  "dependencies": {
    "@types/node": "^14.6.4",
    "amazon-dax-client": "^1.2.4",
    "aws-sdk": "^2.746.0"
  }
}
